<Window x:Class="VPet.Plugin.Claude.PresetNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save as Preset"
        Height="170" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FFF5F5F5">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="txtLabel"
                   Text="Preset name:"
                   FontWeight="SemiBold" Margin="0,0,0,6"/>

        <TextBox Grid.Row="1" x:Name="txtInput"
                 Height="28" Padding="4,2"
                 KeyDown="TxtInput_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="btnOk" Content="OK"
                    Width="80" Height="28" Margin="0,0,8,0"
                    IsDefault="True"
                    Background="#FF3F7FA0" Foreground="White"
                    Click="BtnOk_Click"/>
            <Button x:Name="btnCancel" Content="Cancel"
                    Width="80" Height="28"
                    IsCancel="True"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
